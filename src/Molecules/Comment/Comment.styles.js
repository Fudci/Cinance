import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        marginBottom: 20,
      },
      wrapperSwitch: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        gap: 5,
      },
      listComment: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        gap: 10,
      },
      rowbottomComment: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        gap: 10,
      },
});
